import React from 'react';
import Button from '../components/Button';

function ComponentDemo() {
    return (
        <div>
            <Button mode="rightIcon" color="primary" size="large" icon="home">
                Hello
            </Button>
        </div>
    );
}

export default ComponentDemo;
